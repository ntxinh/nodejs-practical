const { createTodo } = require('./createTodo');
const { checkTodo } = require('./checkTodo');
const { deleteTodo } = require('./deleteTodo');

module.exports = {
  createTodo,
  checkTodo,
  deleteTodo,
};
